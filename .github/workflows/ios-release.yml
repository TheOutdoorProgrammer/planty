name: iOS release

on:
  workflow_dispatch:
    inputs:
      scope:
        description: What to cut
        type: choice
        default: Release
        options: [Release, Release Candidate]
      bump:
        description: Which part of the version to bump
        type: choice
        default: patch
        options: [patch, minor, major]
      dry-run:
        description: Build and sign, publish nothing, cut no tag
        type: boolean
        default: false

permissions:
  contents: read

jobs:
  release:
    # Xcode 26 is the default here, which the iOS 26 deployment target needs.
    runs-on: macos-26
    permissions:
      contents: write
      # Publishing with a workload identity token, so no upload token is
      # stored in this repository at all.
      id-token: write
    steps:
      - uses: actions/checkout@v5
        with:
          # Not optional: quill reads the tag list to work out the next version
          # and refuses a shallow checkout rather than restarting from zero.
          fetch-depth: 0

      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Toolchain
        run: xcodebuild -version

      - name: Test
        working-directory: ios
        run: |
          xcodebuild test \
            -scheme Planty \
            -destination 'platform=iOS Simulator,name=iPhone 17'

      # Automatic signing needs an account, and a runner has no Xcode session
      # to borrow one from, so it authenticates with an App Store Connect key.
      - name: App Store Connect key
        env:
          ASC_KEY_P8: ${{ secrets.ASC_KEY_P8 }}
          ASC_KEY_ID: ${{ secrets.ASC_KEY_ID }}
        run: |
          mkdir -p "$RUNNER_TEMP/keys"
          printf '%s' "$ASC_KEY_P8" > "$RUNNER_TEMP/keys/AuthKey_$ASC_KEY_ID.p8"

      - name: Archive and export
        working-directory: ios
        env:
          ASC_ISSUER_ID: ${{ secrets.ASC_ISSUER_ID }}
          ASC_KEY_ID: ${{ secrets.ASC_KEY_ID }}
          TEAM: ${{ secrets.APPLE_TEAM_ID }}
          PLANTY_BASE_URL: ${{ secrets.PLANTY_BASE_URL }}
          FLEDGE_BASE_URL: ${{ secrets.FLEDGE_URL }}
          # Monotonic and never reused, which is all a build number owes anyone.
          BUILD_NUMBER: ${{ github.run_number }}
        run: |
          set -euo pipefail
          auth=(
            -allowProvisioningUpdates
            -authenticationKeyPath "$RUNNER_TEMP/keys/AuthKey_$ASC_KEY_ID.p8"
            -authenticationKeyID "$ASC_KEY_ID"
            -authenticationKeyIssuerID "$ASC_ISSUER_ID"
          )

          # The build number comes from the run, not the project file. Hand
          # editing it meant a release could ship a build indistinguishable
          # from the one before it, which is what happened: fledge listed two
          # different builds as 1.0 (5) and the update check offered neither.
          xcodebuild archive \
            -scheme Planty \
            -archivePath "$RUNNER_TEMP/Planty.xcarchive" \
            -destination 'generic/platform=iOS' \
            "${auth[@]}" \
            DEVELOPMENT_TEAM="$TEAM" \
            CURRENT_PROJECT_VERSION="$BUILD_NUMBER" \
            PLANTY_BASE_URL="$PLANTY_BASE_URL" \
            FLEDGE_BASE_URL="$FLEDGE_BASE_URL"

          cat > "$RUNNER_TEMP/export.plist" <<PLIST
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0"><dict>
            <key>method</key><string>release-testing</string>
            <key>teamID</key><string>$TEAM</string>
            <key>signingStyle</key><string>automatic</string>
            <key>destination</key><string>export</string>
          </dict></plist>
          PLIST

          xcodebuild -exportArchive \
            -archivePath "$RUNNER_TEMP/Planty.xcarchive" \
            -exportPath "$RUNNER_TEMP/export" \
            -exportOptionsPlist "$RUNNER_TEMP/export.plist" \
            "${auth[@]}"

          ls -la "$RUNNER_TEMP/export"

      - uses: TheOutdoorProgrammer/quill@v1
        id: release
        with:
          bump: ${{ inputs.bump }}
          scope: ${{ inputs.scope }}
          dry-run: ${{ inputs.dry-run }}
          # Quill owns the tag and publishes both downloadable artifacts with
          # one version. The Linux-only image build consumes that version in
          # the reusable job below after Quill succeeds.
          publish: goreleaser, fledge
          fledge-server: ${{ secrets.FLEDGE_URL }}
          # Checked before the tag is cut, so a missing archive does not burn
          # the version. Named, never globbed: this path is opened verbatim,
          # and `*.ipa` is simply a filename that does not exist.
          fledge-ipa: ${{ runner.temp }}/export/Planty.ipa
          # The tap lives in another repository. Quill mints a scoped,
          # hour-long App token rather than storing a broad PAT.
          tap-app-id: ${{ secrets.TAP_APP_ID }}
          tap-private-key: ${{ secrets.TAP_APP_PRIVATE_KEY }}

      - name: Where it landed
        if: steps.release.outputs.page-url != ''
        run: |
          echo "Install page: ${{ steps.release.outputs.page-url }}" >> "$GITHUB_STEP_SUMMARY"

    outputs:
      version: ${{ steps.release.outputs.version }}
      prerelease: ${{ steps.release.outputs.prerelease }}

  backend:
    name: Backend image
    needs: release
    if: inputs.dry-run != true
    uses: ./.github/workflows/release.yml
    with:
      version: ${{ needs.release.outputs.version }}
      prerelease: ${{ needs.release.outputs.prerelease == 'true' }}
    permissions:
      contents: read
      packages: write
